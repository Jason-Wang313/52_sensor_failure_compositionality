# Claims

- Sensor failures in multimodal robot perception are not generally additive.
- Models tuned on isolated failures can fail badly on composed failures.
- A compositional evaluation grid is necessary to expose hidden brittleness.
- A simple interaction model can predict degradation better than independent failure baselines on a controlled synthetic benchmark.
